export class DTInstance {
  id: string;
  DataTable: any;
  dataTable: any;

  constructor(id: string, DataTable: string, dataTable: string) {
    this.id = id;
    this.DataTable = DataTable;
    this.dataTable = dataTable;
  }
}
